# Marks experiments/ so run subdirs share one project root. The Typst format and
# template partials are NOT here — single-file renders ignore project-level
# template-partials. They are injected per-run via --metadata-file ../_typst.yml.
# See run_experiment.sh and README.md.
project:
  type: default
